using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class hit_symbol : MonoBehaviour
{
    public GameObject castingBar;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*if (!castingBar.bite)
        {
            Destroy(gameObject);
        }*/
    }
}
